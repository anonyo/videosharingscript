<div class="container">
  
  <blockquote>
    
  </blockquote>
  
  <%= simple_form_for @post, :html => { :multipart => true } do |f| %>
      <%= f.input :title %>
      <%= f.input :description %> 
      
      <%= form_for @post, :url => posts_path, :html => { :multipart => true } do |form| %>
<%= form.file_field :attachment %></br></br>
      
      <%= f.button :submit %>
        <% end %>
  <% end %>
  
</div>

